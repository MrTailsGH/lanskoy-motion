# Дорожки

Сюда кладутся mp3 из ElevenLabs, по одной на ролик: `I-01.mp3`, `I-02.mp3`.

Имя важно: workflow рендера берёт дорожку отсюда по имени, а текст для
выравнивателя — из `text-I-01.txt` рядом с ним.

Тишину в начале и конце не обрезать: `align.py` по ней ориентируется.
